import React from 'react'

const NotFoundPage = () => (
  <div className="container mx-auto p-4 text-center">
    <h2 className="text-xl font-semibold mb-2">404 — Página no encontrada</h2>
    <p className="text-gray-600">Lo sentimos, no existe la ruta solicitada.</p>
  </div>
)

export default NotFoundPage
